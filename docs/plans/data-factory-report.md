# 测试数据工厂 · 夜间生产报告（2026-08-22 晨）

> 执行：Hermes 编排 → herdr 驱动 opencode（w5:p3）批量生产 → Hermes 逐批独立验收
> 状态：**全部完成**。5 个批次、25 本书、56 篇知识文档，全部入库 READY，已推送 GitHub。

## 一、交付总览

| 项目 | 数量 | 说明 |
|---|---|---|
| 新书（含正文） | **25 本 × 5 章 = 125 章 / 约 2100 内容块 / 约 16 万字** | 全部通过结构校验 + 人工抽查 |
| 知识库文档 | **56 篇**（800~1500 字/篇） | 全部 READY，进入 RAG 检索 |
| 管道设施 | validate_library.py + import_library.py + 语料库格式规范 | 可持续生产，幂等导入 |

## 二、书单（25 本，7 大主题域 × 3 年级段）

- **编程（6）**：Python 第一步 / Scratch 趣味实验室(小学) / 用 Scratch 做游戏(小学) / Python 数据游乐园 / 用 Python 画世界 / 提示词入门(高中)
- **AI 基础（4）**：AI 不是魔法·初中进阶 / 和 AI 做朋友(小学) / 机器怎么学习？(高中HARD) / AI 创意工作室·生成式AI(高中)
- **数据素养（4）**：数据侦探 / 表格里的数学脑 / 我的隐私我做主 / 智慧城市探秘
- **算法思维（3）**：藏在生活里的算法(高中) / 计算思维 / 改变世界的伟大算法(高中HARD)
- **机器人（3）**：机器人会怎么想？ / 动手做机器人 / 机器人的心与责任(高中)
- **AI 伦理/网络素养（5）**：AI 伦理入门 / 网络世界生存指南 / 互联网是怎么工作的(高中) / AI 安全小卫士(小学) / 搜索引擎的秘密

## 三、质量验收记录（每批都过三道关）

1. **结构校验器硬卡**（每批 RESULT: PASS, violations=0）：章≥12块、KC≥2、CALL≥1、FIG≥1、全书≥6000 CJK 字、无占位词；
2. **人工抽查**：每批随机抽读段落，样例——对抗样本（熊猫→长臂猿）、DNS 四问接力、损失函数极简伪代码、非对称加密"装盒/开盒规则"类比、小雨弹窗故事（小学防骗）、turtle 填充翻车排查——事实全部准确、年级段语言适配；
3. **DB 实测对账**：56/56 文档 READY 零缺失；幂等重跑验证通过。

## 四、管道资产（可持续）

```
backend/data/library/          # 语料库源文件（git 管理）
  books/<slug>/book.json + chNN.md
  knowledge/*.md               # front matter 规范
backend/app/scripts/validate_library.py   # 结构门禁
backend/app/scripts/import_library.py     # 幂等导入
```
以后加书 = 让 agent 写文件 → validate → import，三步完成。

## 五、遗留事项（需你裁决，未动手）

1. **380 本测试垃圾书**（"发布测试书-xxx"等）+ 235 个"测试"知识资源：因 1614 个测验会话 FK 引用不能直接删。方案建议：`status='ARCHIVED'` 归档隐藏（前端按 PUBLISHED 过滤即消失），零风险可回滚。你点头我就执行。
2. 12 本原始种子书中除《AI 不是魔法》外仍是占位章节（现在有 25 本新书撑场面，影响小）。
3. embedding 当前是 mock 向量；切真实 provider 后需 `force_reprocess` 重索引。

## 六、Git

5 个提交已推送 origin/master（027b9fc..f570861），工作区干净。
